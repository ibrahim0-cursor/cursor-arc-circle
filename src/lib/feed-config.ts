/** Live feed: fixed roster size — same tokens refresh in place (prices/signals update) */
export const STABLE_FEED_LIMIT = 30;

/** Memory scan: deep archive saved to Agent Memory tab */
export const MEMORY_SCAN_LIMIT = 20;

/** Alpha scan: ranked opportunity pass (news + on-chain + AI) */
export const ALPHA_SCAN_LIMIT = 30;
